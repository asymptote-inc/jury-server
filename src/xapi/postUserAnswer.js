const request = require('superagent')

const Answer = require('../models/answer');
const Question = require('../models/question');
const api = require('../api/crowd9Api');

function postUserAnswer(userId, body, callback) {
  
}